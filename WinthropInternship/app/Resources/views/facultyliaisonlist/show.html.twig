{% extends 'innerbase.html.twig' %}

{% block body %}
    <h1>Faculty Liaison Information</h1>
    <div class="section no-pad-bot" id="index-banner">
        <div class="container">
            <div class="card-panel">
                <table>
                    <tbody>
                        <tr>
                            <th>Id</th>
                            <td>{{ facultyLiaisonList.id }}</td>
                        </tr>
                        <tr>
                            <th>Name</th>
                            <td>{{ facultyLiaisonList.name }}</td>
                        </tr>
                        <tr>
                            <th>Username</th>
                            <td>{{ facultyLiaisonList.username }}</td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div> 
    <ul>
        <li>
            <a href="{{ path('facultyliaisonlist_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('facultyliaisonlist_edit', { 'id': facultyLiaisonList.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
