{% extends 'innerbase.html.twig' %}

{% block title %}Add New Faculty Liaison{% endblock %}

{% block body %}
    <h1 class="header center">Add New Faculty Liaison</h1>
    <div class="section" id="nav-backBtn">
        <ul>
            <li>
                <a href="{{ path('facultyliaisonlist_index') }}" class="btn waves-effect waves-light button-back">Back to the list</a>
            </li>
        </ul>
    </div>
    <div class="section no-pad-bot" id="index-banner">
        <div class="container">
            <div class="card-panel">
            {{ form_start(form) }}
                {{ form_widget(form) }}
                <input type="submit" value="Create" />
            {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
